# AI 问答界面重构 + 全站写作悬浮窗 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不改通用样式（`page-header`/`bg`/`page-container`）的前提下，重构 `/ai-chat` 页面视觉层级，并新增“全站可呼出”的 AI 写作助手悬浮窗。

**Architecture:** 保留问答逻辑在 `AiChat` 页面，仅展示问答主流程；将写作助手逻辑迁移到全局组件 `AiWriterFloat`，挂载在 `App.vue`，实现全站入口。UI 采用“主界面简洁 + 工具浮窗高效”的双层视觉模型，桌面端用右下浮窗，移动端降级为底部抽屉。

**Tech Stack:** Vue 3 + TypeScript + Element Plus + 现有 `aiChat`/`aiWriteAssistStream` API

---

## File Structure

- Create: `src/components/AiWriterFloat/index.vue`  
  全站悬浮按钮 + 写作浮窗 + 写作流式交互（扩写/润色/总结/停止/清空）。
- Modify: `src/App.vue`  
  全局挂载 `<AiWriterFloat />`，保证任意路由可呼出。
- Modify: `src/views/AiChat/index.vue`  
  删除写作助手模块，仅保留问答流程并做高颜值重排（局部 scoped 样式，不触碰通用样式类）。

---

### Task 1: 挂载全站写作悬浮组件骨架

**Files:**
- Create: `src/components/AiWriterFloat/index.vue`
- Modify: `src/App.vue`
- Test: `npm run build`

- [ ] **Step 1: 写一个失败验证（组件未挂载前）**

Run: `npm run build`
Expected: PASS（当前基线可构建），且页面无“AI 写作”全局浮窗入口。

- [ ] **Step 2: 在 `App.vue` 添加全局组件挂载点（先让实现缺失以触发失败）**

在 `src/App.vue` 模板中，`<Tool></Tool>` 后添加：

```vue
<Tool></Tool>
<AiWriterFloat></AiWriterFloat>
<Search></Search>
```

- [ ] **Step 3: 运行构建，验证失败点明确**

Run: `npm run build`
Expected: FAIL，报错类似 `Failed to resolve component: AiWriterFloat`。

- [ ] **Step 4: 创建最小可运行的 `AiWriterFloat` 占位实现使其通过**

创建 `src/components/AiWriterFloat/index.vue`：

```vue
<template>
  <div class="ai-writer-float" aria-label="ai-writer-float-entry">
    <button class="float-btn" type="button">AI 写作</button>
  </div>
</template>

<script setup lang="ts"></script>

<style scoped>
.ai-writer-float {
  position: fixed;
  right: 24px;
  bottom: 24px;
  z-index: 20;
}

.float-btn {
  border: none;
  border-radius: 999px;
  padding: 10px 16px;
  cursor: pointer;
}
</style>
```

- [ ] **Step 5: 运行构建，确认骨架通过**

Run: `npm run build`
Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add src/App.vue src/components/AiWriterFloat/index.vue
git commit -m "feat: add global ai writer float entry"
```

---

### Task 2: 实现全站写作悬浮窗完整交互

**Files:**
- Modify: `src/components/AiWriterFloat/index.vue`
- Reuse: `src/api/ai/index.ts`, `src/api/ai/types.ts`
- Test: `npm run build`

- [ ] **Step 1: 写一个失败验证（先引用未实现状态字段）**

在 `src/components/AiWriterFloat/index.vue` 模板临时加入：

```vue
<div v-if="panelOpen" class="writer-panel"></div>
```

（此时 `panelOpen` 未定义）

- [ ] **Step 2: 运行构建验证失败**

Run: `npm run build`
Expected: FAIL，报错类似 `Cannot find name 'panelOpen'`。

- [ ] **Step 3: 实现完整脚本逻辑（状态、流式、停止、关闭）**

将 `script setup` 实现为：

```ts
import { aiWriteAssistStream } from "@/api/ai";
import type { AiWriteAssistAction } from "@/api/ai/types";

const panelOpen = ref(false);
const fullscreen = ref(false);
const writeContent = ref("");
const writeResult = ref("");
const writing = ref(false);
const currentAction = ref<AiWriteAssistAction | "">("");
let writeAbortController: AbortController | null = null;

const stopWriteStream = () => {
  if (writeAbortController) {
    writeAbortController.abort();
    writeAbortController = null;
  }
  writing.value = false;
  currentAction.value = "";
};

const openPanel = () => {
  panelOpen.value = true;
};

const closePanel = () => {
  if (writing.value) {
    stopWriteStream();
    window.$message?.info("已停止生成");
  }
  panelOpen.value = false;
  fullscreen.value = false;
};

const handleWriteAssist = async (action: AiWriteAssistAction) => {
  const content = writeContent.value.trim();
  if (!content) {
    window.$message?.warning("请输入写作内容");
    return;
  }

  stopWriteStream();
  writeResult.value = "";
  writing.value = true;
  currentAction.value = action;
  writeAbortController = new AbortController();

  try {
    await aiWriteAssistStream(
      { action, content },
      {
        signal: writeAbortController.signal,
        onMessage: (chunk) => {
          writeResult.value += chunk;
        },
        onComplete: () => {
          writing.value = false;
          currentAction.value = "";
          writeAbortController = null;
        },
      },
    );
  } catch (error: any) {
    if (error?.name === "AbortError") {
      return;
    }
    window.$message?.error("写作助手请求失败，请稍后重试");
  } finally {
    if (writing.value) {
      writing.value = false;
      currentAction.value = "";
      writeAbortController = null;
    }
  }
};

const handleStopWrite = () => {
  if (!writing.value) return;
  stopWriteStream();
  window.$message?.info("已停止生成");
};

const handleClearWrite = () => {
  writeContent.value = "";
  writeResult.value = "";
};

onBeforeUnmount(() => {
  stopWriteStream();
});
```

- [ ] **Step 4: 实现完整模板（按钮 + 浮窗 + 移动端抽屉）**

关键结构（保留类名一致，便于后续样式实现）：

```vue
<template>
  <div class="ai-writer-float">
    <button class="float-btn" type="button" @click="openPanel">
      <span class="float-btn-text">AI 写作</span>
    </button>

    <transition name="panel-fade-up">
      <section v-if="panelOpen" class="writer-panel" :class="{ fullscreen }">
        <header class="panel-header">
          <h3>AI 写作助手</h3>
          <div class="header-actions">
            <button type="button" @click="fullscreen = !fullscreen">{{ fullscreen ? "退出全屏" : "全屏" }}</button>
            <button type="button" @click="closePanel">关闭</button>
          </div>
        </header>

        <el-input
          v-model="writeContent"
          type="textarea"
          :rows="5"
          maxlength="2000"
          show-word-limit
          :disabled="writing"
          placeholder="请输入待处理文本"
        />

        <div class="write-actions">
          <el-button type="primary" :loading="writing && currentAction === 'expand'" :disabled="writing" @click="handleWriteAssist('expand')">扩写</el-button>
          <el-button type="primary" :loading="writing && currentAction === 'polish'" :disabled="writing" @click="handleWriteAssist('polish')">润色</el-button>
          <el-button type="primary" :loading="writing && currentAction === 'summary'" :disabled="writing" @click="handleWriteAssist('summary')">总结</el-button>
          <el-button :disabled="!writing" @click="handleStopWrite">停止</el-button>
          <el-button :disabled="writing" @click="handleClearWrite">清空</el-button>
        </div>

        <div class="write-result" :class="{ streaming: writing }">
          {{ writeResult || "生成结果会在这里实时显示" }}
        </div>
      </section>
    </transition>
  </div>
</template>
```

- [ ] **Step 5: 实现高质感样式（按钮玻璃感、浮窗层次、响应式）**

在同文件 `style scoped` 实现：
- `.ai-writer-float` 使用 `position: fixed; right/bottom`，`z-index` 高于 `.tool`（当前 `9`）
- `.float-btn` 胶囊形态、渐变描边、hover 抬升、写作中呼吸态
- `.writer-panel` 桌面端 `width: 420px; height: 560px`
- `.writer-panel.fullscreen` 铺满可视区域留安全边距
- `@media (max-width: 768px)` 转为底部抽屉样式（`width: calc(100vw - 24px); height: 82vh`）

- [ ] **Step 6: 构建验证**

Run: `npm run build`
Expected: PASS。

- [ ] **Step 7: 手工验收（全站可呼出）**

Run: `npm run dev`
Expected:
- 任意页面右下都有“AI 写作”按钮
- 打开后可执行扩写/润色/总结
- 关闭后仅保留按钮
- 移动端宽度下浮窗变为底部抽屉样式

- [ ] **Step 8: Commit**

```bash
git add src/components/AiWriterFloat/index.vue
git commit -m "feat: implement global floating ai writer panel"
```

---

### Task 3: 重构 AI 问答页为高颜值主流程

**Files:**
- Modify: `src/views/AiChat/index.vue`
- Test: `npm run build`

- [ ] **Step 1: 写失败验证（先删除模板中的写作区但不删变量）**

在 `src/views/AiChat/index.vue` 删除 `write-assist-box` 对应模板块，但保留原写作相关脚本变量不改。

- [ ] **Step 2: 运行构建，确认出现未使用/类型问题（或 lint 风险）**

Run: `npm run build`
Expected: PASS 或出现可定位问题（根据 TS 配置），记录并继续清理。

- [ ] **Step 3: 清理页面脚本，只保留问答状态和方法**

`script setup` 最终保留：

```ts
import { aiChat } from "@/api/ai";
import type { AiSource } from "@/api/ai/types";
import Waves from "@/components/Waves/index.vue";

const question = ref("");
const submitting = ref(false);
const answer = ref("");
const sources = ref<AiSource[]>([]);

const handleSubmit = async () => {
  const content = question.value.trim();
  if (!content) {
    window.$message?.warning("请输入问题");
    return;
  }

  submitting.value = true;
  try {
    const { data } = await aiChat({ question: content });
    answer.value = data?.data?.answer ?? "";
    sources.value = data?.data?.sources ?? [];
  } catch {
    window.$message?.error("提问失败，请稍后重试");
  } finally {
    submitting.value = false;
  }
};

const handleClear = () => {
  question.value = "";
  answer.value = "";
  sources.value = [];
};
```

- [ ] **Step 4: 重写模板结构为问答单主线布局**

目标模板结构：

```vue
<div class="bg">
  <div class="page-container">
    <section class="ai-shell">
      <header class="ai-hero">...</header>
      <section class="qa-card">...</section>
      <section v-if="answer" class="answer-card">...</section>
      <section v-if="sources.length" class="source-card">...</section>
    </section>
  </div>
</div>
```

保留原有通用区块：`page-header`、`page-title`、`page-cover`、`Waves`、`bg`、`page-container`。

- [ ] **Step 5: 重写 `scoped` 样式（只改内容层）**

在 `src/views/AiChat/index.vue` 中：
- 删除旧 `.write-assist-box/.write-actions/.write-result` 样式
- 新增 `.ai-shell/.qa-card/.answer-card/.source-card` 体系
- 使用克制玻璃卡片 + 柔和阴影 + 渐变描边 + 响应式间距
- 链接样式和 hover 细节统一

- [ ] **Step 6: 构建验证**

Run: `npm run build`
Expected: PASS。

- [ ] **Step 7: 手工验收（视觉与可用性）**

Run: `npm run dev`
Expected:
- `/ai-chat` 页面仅展示问答主流程，明显更简洁
- 问答输入、提交、清空、回答显示、来源跳转都正常
- 与通用头图和容器样式无冲突

- [ ] **Step 8: Commit**

```bash
git add src/views/AiChat/index.vue
git commit -m "feat: redesign ai chat page with focused qa layout"
```

---

### Task 4: 联调验收与交付说明

**Files:**
- Modify: `docs/前端升级/2026-04-21-AI聊天写作浮窗-实施计划.md`（勾选完成项）
- Test: `npm run build`, `npm run dev`

- [ ] **Step 1: 全量构建验证**

Run: `npm run build`
Expected: PASS（无 TS 构建错误）。

- [ ] **Step 2: 全站回归验证**

Run: `npm run dev`
Expected:
- 全站有悬浮入口
- `/ai-chat` 简洁化达成
- 写作流式中断与异常提示正常
- 移动端布局无明显遮挡

- [ ] **Step 3: 按用户要求合并提交文档与界面优化**

```bash
git add src/App.vue src/components/AiWriterFloat/index.vue src/views/AiChat/index.vue docs/前端升级/2026-04-21-AI聊天写作浮窗-实施计划.md
git commit -m "feat: revamp ai chat ui and add global floating writer assistant"
```

---

## Self-Review

- **Spec coverage:**
  - 全站悬浮入口（Task 1 + Task 2）✅
  - 默认仅按钮（Task 2）✅
  - 问答页去臃肿（Task 3）✅
  - 保持通用样式不重声明（Task 3 约束）✅
  - 响应式与移动端降级（Task 2/3）✅
- **Placeholder scan:** 已移除 TBD/TODO/“后续实现”等占位语。✅
- **Type consistency:** `AiWriteAssistAction`、`aiWriteAssistStream`、问答状态命名在任务内保持一致。✅
